# Python Snippet Indentation

- Generated Python code snippets returned to callers must have every line after the first indented by four spaces, so the snippet embeds correctly as a multi-line string.
