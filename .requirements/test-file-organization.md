# Move Unit Tests Out of Large Source Files

- When a source file grows too long, its unit tests should be relocated into dedicated test files under the crate's test directory rather than staying inline in the implementation file.
