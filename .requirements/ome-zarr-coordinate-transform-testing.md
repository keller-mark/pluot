# OME-Zarr Coordinate Transform Validation

- Coordinate transformation logic for OME-Zarr data must be validated against a community reference set of example metadata (tracked as a dependency of the project), with tests built from that reference metadata rather than hand-invented cases.
