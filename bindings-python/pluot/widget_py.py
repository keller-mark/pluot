# TODO: implement an anywidget that performs rendering via Pluot's python render function(s), and renders the result to an HTML canvas element.
# I.e., use backend/server-side rendering.
# The graphics bytes should be transmitted via the Buffer part of the widget communication protocol message.
# It should leverage the camera implementation from @pluot/core for interactivity.
