This pluot package will depend on pluot_core for any common functionality that the pluot_widget also relies on, such as zarr and font stuff and viewport utilities. However, this package will be the only package among the three (pluot, pluot_core, pluot_widget) which requires the Rust bindings and allows for rendering plots via the Rust bindings.
