---
"@pluot/react": patch
"@pluot/core": patch
---

Add an extent trait and extent_wasm bindings for obtaining initial camera settings when not fully specified up-front by the user. Update the histogram to use data units along its X-axis so that brushing can be performed in data-units. Update taxi and flights examples.
