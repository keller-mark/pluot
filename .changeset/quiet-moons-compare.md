---
"@pluot/core": patch
---

Support exclusive (`>`/`<`) bounds on quantitative filtering/selection criteria via `min_exclusive`/`max_exclusive`, and skip the comparison (and the value texture upload) for omitted bounds.
