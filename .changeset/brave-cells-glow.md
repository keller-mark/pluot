---
"@pluot/core": patch
---

Add HeatmapLayer and AdataZarrHeatmapLayer, with per-axis filtering and selection by identifiers or zarr criteria (including a new Boolean criteria), whole-matrix/per-obs/per-var colormap normalization, and a per-var column cache mode.
