This crate provides functionality for rendering visualizations.

This `pluot` root crate pulls in the `pluot_core` and format-specific crates such as `pluot_zarr`, and is used by the non-Rust programming language bindings.
