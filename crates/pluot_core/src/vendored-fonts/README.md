We copy fonts here so that they can be loaded via `include_bytes!` statements.
The copying is performed in `build.rs`.

This is analogous to the copy step in `bindings-js/core/scripts/copy-fonts.mjs`.
