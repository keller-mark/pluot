[toolchain]
channel = "1.93.0" # works if not using +atomics in .cargo/config.toml
# channel = "nightly-2026-02-11" # works with +atomics
# channel = "nightly-2025-12-30" # works with +atomics
# channel = "nightly-2025-11-15" # works with +atomics
# channel = "1.93.0-beta"        # doesnt work with +atomics
# channel = "1.92.0"             # doesnt work with +atomics
components = ["rust-src"]
targets = ["wasm32-unknown-unknown"]
