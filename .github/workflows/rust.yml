name: Rust

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  unit_tests:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4

    # Checkout main branch of Zarrs
    - uses: actions/checkout@v4
      with:
        repository: zarrs/zarrs
        ref: main
        path: ./zarrs

    - uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable

    - uses: Swatinem/rust-cache@v2
    - name: Compile Rust code
      run: cargo build --verbose
    
    # TODO: implement plain-rust unit tests.
