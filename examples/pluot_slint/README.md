# pluot GUI example with Slint

This example is intentionally kept very similar to the `egui` example.

```sh
# In this directory
cargo build
cargo run
```

```sh
# Or, from the repo root
cargo run --manifest-path examples/pluot_slint/Cargo.toml
```
