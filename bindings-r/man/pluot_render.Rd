% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{pluot_render}
\alias{pluot_render}
\title{Render a layered plot to raw bytes}
\usage{
pluot_render(
  layers,
  width,
  height,
  format = "Raster",
  device_pixel_ratio = 1,
  camera_view = NULL,
  aspect_ratio_mode = "Contain",
  aspect_ratio_alignment_mode = "Center",
  view_mode = "2d",
  plot_id = "",
  store_name = "",
  wait_for_store_gets = TRUE,
  timeout = NULL,
  cache_enabled = TRUE,
  svg_compression_enabled = FALSE,
  svg_include_document = TRUE,
  margin_left = NULL,
  margin_right = NULL,
  margin_top = NULL,
  margin_bottom = NULL,
  pickable = FALSE,
  render_backend = NULL,
  compute_backend = NULL
)
}
\arguments{
\item{layers}{A list of layer definitions.}

\item{width}{Output width in pixels (integer).}

\item{height}{Output height in pixels (integer).}

\item{format}{Graphics format: `"Raster"` (default) or `"Vector"` (SVG).}

\item{device_pixel_ratio}{Device pixel ratio (default 1.0).}

\item{camera_view}{Optional 16-element numeric vector (column-major 4x4
matrix). `NULL` uses the default camera.}

\item{aspect_ratio_mode}{One of `"Contain"` (default), `"Cover"`, `"Fill"`.}

\item{aspect_ratio_alignment_mode}{One of `"Center"` (default), `"Start"`,
`"End"`.}

\item{view_mode}{`"2d"` (default) or `"3d"`.}

\item{plot_id}{Identifier string used as a cache key (default `""`).}

\item{store_name}{Name of a registered Zarr store (default `""`).}

\item{wait_for_store_gets}{Wait for in-flight store requests (default `TRUE`).}

\item{timeout}{Optional render timeout in milliseconds. `NULL` means no
timeout.}

\item{cache_enabled}{Enable render cache (default `TRUE`).}

\item{svg_compression_enabled}{Compress SVG output (default `FALSE`).}

\item{svg_include_document}{Wrap SVG in an XML document header (default
`TRUE`).}

\item{margin_left, margin_right, margin_top, margin_bottom}{Optional margins in
pixels. `NULL` means no margin.}

\item{pickable}{Enable picking (default `FALSE`).}

\item{render_backend}{Optional render backend string. `NULL` uses the
default.}

\item{compute_backend}{Optional compute backend string. `NULL` uses the
default.}
}
\value{
A raw vector of bytes.
}
\description{
Calls the Rust pluot rendering engine and returns the result as a raw vector.
For raster output (default), the bytes are RGBA pixels (width * height * 4
bytes, plus one trailing status byte). For vector output, the bytes are a
UTF-8 encoded SVG string.
}
\details{
Each layer in `layers` must be a named list with at minimum:
\describe{
  \item{layer_type}{A string matching a registered layer name, e.g.
    `"PointLayer"`, `"BarPlotLayer"`, `"OmeZarrBitmapLayer"`, etc.}
  \item{layer_params}{A named list of layer-specific parameters.}
}
}
